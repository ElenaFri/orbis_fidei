# Documentation API

La documentation OpenAPI sera générée automatiquement à partir des schémas Zod
enregistrés dans Fastify (via `@fastify/swagger`) — à intégrer en Phase 2.

En attendant, se référer aux fichiers `apps/api/src/*/routes.ts`.
